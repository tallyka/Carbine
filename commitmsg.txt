BUILD 198: Remove all outbound telemetry webhooks

- Removed the hardcoded launch-analytics POST to a real Discord webhook
  (reported player name/UserId/executor/place/job on every launch)
- Neutered setup_error_webhook (reported script errors + Discord ID/key
  to a devs' webhook) and flag_chat/noob chat surveillance
- Removed the artifact-find telemetry stream/secondary webhook send
  (queued+POSTed artifact finds to a hardcoded devs' webhook)

User-configurable webhook (Options > Discord Webhook URL, used by
utility:plain_webhook for the user's own notifications) is untouched.
